---
title: "Inicio"
description: "Bienvenido a VIBICO - Viajeros en Bicicleta Por Colombia"
image: "public/imagen-principal.jpg"
---

import config from "../../../config/index.js";
import Home from "../../../components/pages/Home.astro";
import LinkGrid from "../../../components/LinkGrid.astro";
import InfoGrid from "../../../components/InfoGrid.astro";

<Home />

<div class="space-y-12 pb-6">

  <section>
    <div class="text-center mb-8">
      <h2>{config.languages[props.lang].pages.home.contacts_title}</h2>
      <p class="text-sm text-slate-500 max-w-lg mx-auto mt-3">
        {config.languages[props.lang].pages.home.contacts_desc}
      </p>
    </div>
    <LinkGrid data={config.common.contacts} />
  </section>

  <section>
    <div class="text-center mb-10">
      <h2>{config.languages[props.lang].pages.home.what_title}</h2>
      <p class="text-sm text-slate-500 max-w-xl mx-auto mt-3">
        {config.languages[props.lang].pages.home.what_desc}
      </p>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      {config.languages[props.lang].pages.home.what_cards.map((card) => (
        <div class="card p-6 hover:border-cyan-600/30 transition-all">
          <div class="flex items-start gap-4">
            <span class="text-3xl flex-shrink-0 mt-1">{card.icon}</span>
            <div>
              <h4 class="text-base font-semibold text-white mb-2">{card.title}</h4>
              <p class="text-sm text-slate-400 leading-relaxed">
                {card.text}
              </p>
            </div>
          </div>
        </div>
      ))}
    </div>
  </section>

  <section>
    <div class="text-center mb-8">
      <h2>{config.languages[props.lang].pages.home.social_title}</h2>
      <p class="text-sm text-slate-500 max-w-lg mx-auto mt-3">
        {config.languages[props.lang].pages.home.social_desc}
      </p>
    </div>
    <LinkGrid data={config.common.social} />
  </section>

  <section>
    <div class="card p-8 md:p-10 text-center max-w-xl mx-auto hover:border-cyan-600/30">
      <span class="text-3xl mb-4 block">💸</span>
      <h3 class="text-xl font-bold text-white mb-3">{config.languages[props.lang].pages.home.support_title}</h3>
      <p class="text-sm text-slate-400 leading-relaxed max-w-md mx-auto">
        {config.languages[props.lang].pages.home.support_desc}
      </p>
      <div class="mt-6">
        <LinkGrid data={config.common.payment} />
      </div>
      <p class="text-sm text-slate-500 mt-4"><em>{config.languages[props.lang].pages.home.support_thanks}</em></p>
    </div>
  </section>

  <section>
    <div class="card p-6 border-amber-700/30 bg-amber-900/10 max-w-2xl mx-auto">
      <div class="flex items-start gap-3">
        <span class="text-xl flex-shrink-0 mt-0.5">⚠️</span>
        <div>
          <h4 class="text-base font-bold text-amber-300 mb-2">{config.languages[props.lang].pages.home.important_title}</h4>
          <ul class="text-sm text-amber-200/70 space-y-1.5 list-disc list-inside">
            {config.languages[props.lang].pages.home.important_items.map((item) => (
              <li>{item}</li>
            ))}
          </ul>
        </div>
      </div>
    </div>
  </section>

  <div class="text-center space-y-4">
    <div class="inline-block px-6 py-3 rounded-xl bg-slate-800/50 border border-slate-700/50 text-sm text-slate-500">
      {config.languages[props.lang].pages.home.footer_ad}
    </div>
    <p class="text-sm text-slate-500">
      {config.languages[props.lang].pages.home.footer_tagline}
    </p>
    <blockquote class="text-sm text-slate-600 italic border-l-2 border-slate-700 pl-4 inline-block text-left mt-4">
      {config.languages[props.lang].pages.home.footer_founder}<br />
      <span class="text-slate-500 not-italic">{config.languages[props.lang].pages.home.footer_founder_name}</span>
    </blockquote>
  </div>

</div>
